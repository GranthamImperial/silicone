language: python
cache: pip
dist: xenial
python:
  - "3.5"
  - "3.6"
  - "3.7"

before_install:
  - pip install pip --upgrade

install:
  - pip install -e .[tests]

script:
  - pytest --cov -r a --cov-report term-missing

after_success:
  - codecov
